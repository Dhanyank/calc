
// const calc = (qstn)=>{

  // let formula = req.body.qstn;
  
  // let answer = eval(formula);
  // res.send(`${answer}`);


  // var formula = req.body.qstn;
  
  // var lastKey = qstn[qstn.length - 1];

  // if (lastKey === '.') {
  //     qstn = qstn.substr(0, qstn.length - 1);
  // }

  // lastKey = qstn[qstn.length - 1];

  // if (lastKey === '/' || lastKey === '*' || lastKey === '-' || lastKey === '+' || lastKey === '.') {
  //     qstn = qstn.substr(0, qstn.length - 1);
  // }
  module.exports.calc = (req, res)=>{

{
   this.result = eval(qstn)
 
this.answer =result.calc
    //  return answer 
    res.send('${answer}')
  


}



  }


















// module.exports.calc()
// {
//  let formula = this.input;


//  let lastKey = formula[formula.length - 1];

//  if (lastKey === '.') {
//    formula = formula.substr(0, formula.length - 1);
//  }

//  lastKey = formula[formula.length - 1];

//  if (lastKey === '/' || lastKey === '*' || lastKey === '-' || lastKey === '+' || lastKey === '.') {
//    formula = formula.substr(0, formula.length - 1);
//  }

//  console.log("Formula " + formula);
//  this.result = eval(formula);
//  answer: calc.result


// }


    
       // var formula = req.body.input;
        //var answer = parseInt(answer)

        // var lastKey = formula[formula.length - 1];

        // if (lastKey === '.') {
        //     formula = formula.substr(0, formula.length - 1);
        // }

        // lastKey = formula[formula.length - 1];

        // if (lastKey === '/' || lastKey === '*' || lastKey === '-' || lastKey === '+' || lastKey === '.') {
        //     formula = formula.substr(0, formula.length - 1);
        // }

        // console.log("Formula " + formula);
    //    let  result = eval(input);
                 
        


    // const calc = (input) => {

    
    //           return {
    //             statusCode: 200,
    //             status: true,
    //             transaction: user.transaction
    //           }
      
    //         }
    //         else {
    //           return {
    //             statusCode: 401,
    //             status: false,
    //             message: "User does not exist"
    //           }
      
    //         }
      
    //       })
      
    //   }
      

            
    
